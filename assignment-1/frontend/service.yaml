apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: dso202-assignment-01
  labels:
    tier: frontend
spec:
  type: NodePort
  selector:
    tier: frontend
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080   # placeholder — replace once we know your real mapping, or remove this line to let k8s auto-assign